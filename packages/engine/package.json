{
  "name": "@pantheons/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Pantheons pure rules/state engine — no I/O. Mirrors the WoG engine pattern (Decision 2).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test \".test-dist/**/*.test.js\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
